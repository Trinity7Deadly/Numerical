// while in the main directory /Fionn

	git commit -a -m "the date"
	git push
	
// -a is for all and -m is the message, this will update it daily

// if something isn't working then

	git add NameOfThing
	git commit -a -m "date"
	git push
